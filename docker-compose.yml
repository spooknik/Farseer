version: '3.8'

services:
  farseer:
    build: .
    container_name: farseer
    ports:
      - "8080:8080"
    volumes:
      - farseer-data:/data
    environment:
      - FARSEER_PORT=8080
      - FARSEER_PRODUCTION=true
    restart: unless-stopped

volumes:
  farseer-data:
